package shape;

public class Square {
    // Method to calculate the area of a square (only one side required)
    public void area(long l) {
        System.out.println("Area of Square: " + (l * l));
    }
}
