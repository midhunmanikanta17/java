package shape;

public class Triangle {
    // Method to calculate the area of a triangle (base * height * 0.5)
    public void area(long l, long b) {
        System.out.println("Area of Triangle: " + (l * b * 0.5));
    }
}
